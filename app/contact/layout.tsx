export const metadata: Metadata = {
  title: "Contact Foreware Technologies",
  description:
    "Reach out to Foreware Technologies for IT consulting, enterprise business solutions, or custom software development services	Contact Foreware Technologies",
};

import { Metadata } from "next";
import { PropsWithChildren } from "react";

const ContactLayout = ({ children }: Readonly<PropsWithChildren>) => {
  return <>{children}</>;
};

export default ContactLayout;
