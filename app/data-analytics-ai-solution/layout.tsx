export const metadata: Metadata = {
  title: "AI & Data Analytics Solutions for Business | Foreware",
  description:
    "Turn data into decisions with Foreware's AI and analytics services, business intelligence, data warehousing, predictive analytics, machine learning, and big data solutions in Nigeria.	AI and analytics services",
};

import { Metadata } from "next";
import { PropsWithChildren } from "react";

const DataAnalyticsAISolutionLayout = ({
  children,
}: Readonly<PropsWithChildren>) => {
  return <>{children}</>;
};

export default DataAnalyticsAISolutionLayout;
