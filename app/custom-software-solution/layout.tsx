export const metadata: Metadata = {
  title: "Custom Software Development Services in Nigeria | Foreware",
  description:
    "Foreware builds tailor-made software: web apps, mobile apps, enterprise applications, and legacy modernization, designed to fit your exact business workflows and goals.	custom software development in Nigeria",
};

import { Metadata } from "next";
import { PropsWithChildren } from "react";

const CustomSoftwareSolutionLayout = ({
  children,
}: Readonly<PropsWithChildren>) => {
  return <>{children}</>;
};

export default CustomSoftwareSolutionLayout;
