export const metadata: Metadata = {
  title: "Wondersoft Partner Nigeria | Retail & POS Solutions | Foreware",
  description:
    "As a Wondersoft partner in Nigeria, Foreware helps retailers manage billing, inventory, promotions, and multi-store operations with a proven retail and POS platform.	Wondersoft partner in Nigeria",
};

import { Metadata } from "next";
import { PropsWithChildren } from "react";

const WondersoftLayout = ({ children }: Readonly<PropsWithChildren>) => {
  return <>{children}</>;
};

export default WondersoftLayout;
