export const metadata: Metadata = {
  title: "WebHR Partner in Nigeria | HR Management System | Foreware",
  description:
    "As a WebHR partner in Nigeria, Foreware supports businesses in deploying WebHR for payroll, leave management, performance tracking, and employee records.	WebHR partner in Nigeria",
};

import { Metadata } from "next";
import { PropsWithChildren } from "react";

const WebHRLayout = ({ children }: Readonly<PropsWithChildren>) => {
  return <>{children}</>;
};

export default WebHRLayout;
