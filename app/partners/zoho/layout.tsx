export const metadata: Metadata = {
  title: "Zoho Partner in Nigeria | CRM & Business Apps | Foreware",
  description:
    "Foreware is an authorised Zoho partner in Nigeria, helping sales-driven businesses deploy Zoho CRM for lead management, customer engagement, and pipeline visibility.	Zoho partner in Nigeria",
};

import { Metadata } from "next";
import { PropsWithChildren } from "react";

const ZohoLayout = ({ children }: Readonly<PropsWithChildren>) => {
  return <>{children}</>;
};

export default ZohoLayout;
