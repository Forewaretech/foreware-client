export const metadata: Metadata = {
  title: "Xero Partner in Nigeria | Cloud Accounting Software | Foreware",
  description:
    "Foreware is a Xero partner in Nigeria, helping small and growing businesses manage accounting, invoicing, bank reconciliation, and financial reporting on the cloud.	Xero partner in Nigeria",
};

import { Metadata } from "next";
import { PropsWithChildren } from "react";

const XeroLayout = ({ children }: Readonly<PropsWithChildren>) => {
  return <>{children}</>;
};

export default XeroLayout;
