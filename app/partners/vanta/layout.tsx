export const metadata: Metadata = {
  title: "Vanta GRC Partner in Nigeria | Compliance & Risk | Foreware",
  description:
    "Foreware is an authorised Vanta GRC partner in Nigeria, helping organisations automate compliance monitoring, manage risk, and achieve audit readiness across multiple frameworks.	Vanta partner in Nigeria",
};

import { Metadata } from "next";
import { PropsWithChildren } from "react";

const VentaLayout = ({ children }: Readonly<PropsWithChildren>) => {
  return <>{children}</>;
};

export default VentaLayout;
