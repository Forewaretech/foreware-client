export const metadata: Metadata = {
  title: "Systems Integration & API Development Services | Foreware",
  description:
    "Foreware connects your business platforms through enterprise system integration, custom API design, third-party software integration, and middleware solutions in Nigeria.	API integration services Nigeria",
};

import { Metadata } from "next";
import { PropsWithChildren } from "react";

const SystemIntegrationApiDevLayout = ({
  children,
}: Readonly<PropsWithChildren>) => {
  return <>{children}</>;
};

export default SystemIntegrationApiDevLayout;
