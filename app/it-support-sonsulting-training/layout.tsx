export const metadata: Metadata = {
  title: "IT Support, Consulting & Training Services Nigeria | Foreware",
  description:
    "Foreware provides 24/7 IT support, strategic consulting, cybersecurity advisory, infrastructure management, and user training to keep your business running securely in Nigeria.	IT support services in Nigeria",
};

import { Metadata } from "next";
import { PropsWithChildren } from "react";

const ITSupportConsultingTrainingLayout = ({
  children,
}: Readonly<PropsWithChildren>) => {
  return <>{children}</>;
};

export default ITSupportConsultingTrainingLayout;
